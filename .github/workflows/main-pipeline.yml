name: 每日量化流水线（盘后）

on:
  schedule:
    - cron: '0 8 * * 1-5'   # 工作日北京时间 16:00（UTC 8:00）
  workflow_dispatch:

jobs:
  pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      WORK_DIR: /home/runner/work/quant-workspace
    steps:
      - name: 检出总控仓库
        uses: actions/checkout@v4

      - name: 设置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 安装通用依赖
        run: |
          pip install tickflow tushare pandas

      - name: 拉取/更新项目代码
        run: |
          chmod +x scripts/update_projects.sh
          ./scripts/update_projects.sh

      - name: 注入环境变量到 daily_stock_analysis
        run: |
          cd $WORK_DIR/daily_stock_analysis
          cp .env.example .env || true
          echo "STOCK_LIST=${{ secrets.STOCK_LIST }}" >> .env
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" >> .env
          echo "OPENAI_BASE_URL=${{ secrets.OPENAI_BASE_URL }}" >> .env
          echo "OPENAI_MODEL=${{ secrets.OPENAI_MODEL }}" >> .env
          echo "TUSHARE_TOKEN=${{ secrets.TUSHARE_TOKEN }}" >> .env
          # ===== 新增：TickFlow 优先 =====
          echo "TICKFLOW_PRIORITY=0" >> .env
          echo "AKSHARE_PRIORITY=1" >> .env
          echo "TUSHARE_PRIORITY=2" >> .env
          echo "DATA_SOURCE_PRIORITY=tickflow,akshare,tushare,baostock,yfinance" >> .env
          # =============================
          echo "EMAIL_SENDER=${{ secrets.EMAIL_SENDER }}" >> .env
          echo "EMAIL_PASSWORD=${{ secrets.EMAIL_PASSWORD }}" >> .env
          echo "EMAIL_RECEIVERS=${{ secrets.EMAIL_RECEIVERS }}" >> .env
          echo "SECRET_KEY=$(openssl rand -hex 32)" >> .env

      # ----- 步骤1: 大盘复盘（生成今日信号） -----
      - name: 大盘复盘
        run: |
          cd $WORK_DIR/daily_stock_analysis
          python main.py --market-review --no-notify
        env:
          ALPHASIFT_ENABLED: false

      # ----- 步骤2: 策略自动选择（基于今日信号） -----
      - name: 策略自动选择
        id: select_strategy
        run: |
          python scripts/select_strategy.py
        env:
          WORK_DIR: ${{ env.WORK_DIR }}

      # ----- 步骤3: AlphaSift 选股（独立仓库） -----
      - name: AlphaSift 选股
        run: |
          cd $WORK_DIR/alphasift
          pip install -e . || true
          STRATEGY="${{ steps.select_strategy.outputs.selected_strategy }}"
          echo "使用策略: $STRATEGY"
          alphasift screen $STRATEGY --no-llm --save-run
          # 找到最新生成的 JSON 结果
          latest_json=$(ls -t data/runs/*.json 2>/dev/null | head -1)
          if [ -z "$latest_json" ]; then
            echo "错误：未生成选股结果 JSON"
            exit 1
          fi
          echo "选股结果 JSON: $latest_json"
          # 使用转换脚本生成 CSV
          mkdir -p $WORK_DIR/daily_stock_analysis/data
          python $GITHUB_WORKSPACE/scripts/convert_alphasift_json_to_csv.py "$latest_json" $WORK_DIR/daily_stock_analysis/data/stock_pool.csv
          if [ $? -eq 0 ]; then
            echo "选股结果已保存到 shared/stock_pool.csv"
          else
            echo "错误：转换 CSV 失败"
            exit 1
          fi
        env:
          ALPHASIFT_ENABLED: true

      # ----- 步骤4: AlphaEvo 每日回测校验 -----
      - name: AlphaEvo 每日回测校验
        run: |
          cd $WORK_DIR/alphaevo
          mkdir -p data
          if [ -f "$WORK_DIR/daily_stock_analysis/data/stock_pool.csv" ]; then
            cp $WORK_DIR/daily_stock_analysis/data/stock_pool.csv data/stock_pool_raw.csv
          else
            echo "错误：未找到 stock_pool.csv"
            exit 1
          fi
          python $GITHUB_WORKSPACE/scripts/run_alphaevo.py backtest \
            --input data/stock_pool_raw.csv \
            --output data/stock_pool_final.csv
        env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      # ----- 步骤5: daily_stock_analysis 完整分析推送 -----
      - name: daily_stock_analysis 分析与推送
        run: |
          cd $WORK_DIR/daily_stock_analysis
          cp $WORK_DIR/alphaevo/data/stock_pool_final.csv data/stock_pool_final.csv
          python main.py
        env:
          ALPHASIFT_ENABLED: false

      # ----- 步骤6: AlphaEvo 策略进化（条件触发） -----
      - name: 判断是否运行 AlphaEvo 进化
        id: check_evo
        run: |
          DOW=$(date +%u)
          DOM=$(date +%d)
          if [ "$DOW" = "5" ] || [ "$DOM" = "01" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "run_evo=true" >> $GITHUB_OUTPUT
          else
            echo "run_evo=false" >> $GITHUB_OUTPUT
          fi

      - name: AlphaEvo 策略进化（条件触发）
        if: steps.check_evo.outputs.run_evo == 'true'
        run: |
          cd $WORK_DIR/alphaevo
          DOM=$(date +%d)
          if [ "$DOM" = "01" ]; then
            MODE="optimize --full-scan"
          else
            MODE="evolve --method llm"
          fi
          python $GITHUB_WORKSPACE/scripts/run_alphaevo.py $MODE
        env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}

      # ----- 归档 -----
      - name: 归档所有结果
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: quant-results-${{ github.run_id }}
          path: |
            ${{ env.WORK_DIR }}/daily_stock_analysis/data/
            ${{ env.WORK_DIR }}/daily_stock_analysis/reports/
            ${{ env.WORK_DIR }}/daily_stock_analysis/logs/
            ${{ env.WORK_DIR }}/alphaevo/data/
            ${{ env.WORK_DIR }}/alphaevo/docs/reports/
          retention-days: 30

      # ----- 告警 -----
      - name: 异常告警（邮件）
        if: failure()
        run: |
          python scripts/send_alert.py
        env:
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECEIVERS: ${{ secrets.EMAIL_RECEIVERS }}
          ERROR_MSG: "量化流水线执行失败，请检查 Actions 日志。"